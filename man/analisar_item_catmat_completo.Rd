% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precos-analise.R
\name{analisar_item_catmat_completo}
\alias{analisar_item_catmat_completo}
\title{Analisar preços de um item do CATMAT, em todas as unidades de fornecimento qualificadas}
\usage{
analisar_item_catmat_completo(
  codigo_catmat,
  nome_do_responsavel = "Nome",
  pasta = "PesquisadePrecos",
  salvar = FALSE,
  servico = FALSE,
  meses = 12,
  estado = NULL,
  regiao = NULL,
  return_dados = FALSE,
  unidadeF = NULL,
  ajuste_periodo = TRUE,
  preferencia_modalidade = NULL
)
}
\arguments{
\item{codigo_catmat}{Código do item no catálogo (CATMAT ou serviço).}

\item{nome_do_responsavel}{Nome do responsável pela pesquisa.}

\item{pasta}{Caminho da pasta onde os arquivos serão salvos, quando \code{salvar = TRUE}.}

\item{salvar}{Lógico. Se TRUE, salva os dados e resumo em arquivos Excel por unidade de fornecimento.}

\item{servico}{Se TRUE, consulta serviços em vez de materiais.}

\item{meses}{Número de meses anteriores considerados na busca inicial.}

\item{estado}{Filtrar por estado (opcional).}

\item{regiao}{Filtrar por região (opcional).}

\item{return_dados}{Lógico. Se TRUE, retorna também os dados saneados/excluídos de cada
unidade de fornecimento (ver Value).}

\item{unidadeF}{Vetor de unidades de fornecimento a analisar (opcional). Se \code{NULL},
usa todas as unidades com pelo menos 3 registros.}

\item{ajuste_periodo}{Lógico. Se TRUE, usa o menor período (3/6/9/12 meses) com amostra
de pelo menos 3 preços válidos.}

\item{preferencia_modalidade}{Código da modalidade de licitação preferida (opcional);
só é aplicado se restarem pelo menos 3 registros nessa modalidade.}
}
\value{
Um tibble com uma linha de resumo por unidade de fornecimento analisada ou,
se \code{return_dados = TRUE}, uma lista com:
\itemize{
  \item \code{resumo}: o mesmo tibble de resumo;
  \item \code{detalhes}: lista nomeada por unidade de fornecimento, cada elemento com
    \code{dadosSaneados} e \code{dadosExcluidos}.
}
}
\description{
Versão mais completa de \code{\link{analisar_item_material}}: em vez de considerar
apenas a unidade de fornecimento predominante, analisa \strong{todas} as unidades com
pelo menos 3 registros, suporta materiais e serviços, permite restringir a uma
modalidade de licitação preferida e ajusta automaticamente o período (3/6/9/12 meses)
para o menor intervalo com amostra suficiente.
}
